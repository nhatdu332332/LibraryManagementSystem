<Window x:Class="LibraryManagementSystem.WPF.Views.EditBookView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagementSystem.WPF.Views"
        mc:Ignorable="d"
       Title="Edit Book" Height="350" Width="400">
    <Grid>
        <StackPanel Margin="20">

            <TextBlock Text="Title"/>
            <TextBox Text="{Binding Title}" />

            <TextBlock Text="Original Title"/>
            <TextBox Text="{Binding OriginalTitle}" />

            <TextBlock Text="Summary"/>
            <TextBox Text="{Binding Summary}" Height="60"/>

            <TextBlock Text="Year"/>
            <TextBox Text="{Binding FirstPublishYear}" />

            <Button Content="Save"
                Command="{Binding SaveCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
